---
import Layout from '../../layouts/Layout.astro';
import { getLangFromUrl, useTranslations } from '../../utils/i18n';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<Layout title={t('site.title')} description={t('site.description')}>
  <!-- Hero Section -->
  <section class="section section-hero">
    <div class="container">
      <div class="hero-content fade-in">
        <h1 class="hero-title">{t('hero.title')}</h1>
        <p class="hero-subtitle text-large">{t('hero.subtitle')}</p>
        <div class="hero-actions">
          <a href="#contact" class="btn">{t('hero.cta')}</a>
          <a href="#features" class="btn btn-secondary">{t('hero.cta.secondary')}</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Features Section -->
  <section class="section" id="features">
    <div class="container">
      <div class="section-header text-center mb-4">
        <h2>Transforma tu Negocio con IA</h2>
        <p class="text-large">Descubre ineficiencias, automatiza procesos y libera el potencial de tu empresa</p>
      </div>
      
      <div class="grid grid-3">
        <div class="card fade-in">
          <div class="feature-icon">
            <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M9 12l2 2 4-4"/>
              <path d="M21 12c-1 0-3-1-3-3s2-3 3-3 3 1 3 3-2 3-3 3"/>
              <path d="M3 12c1 0 3-1 3-3s-2-3-3-3-3 1-3 3 2 3 3 3"/>
              <path d="M12 3c0 1-1 3-3 3s-3-2-3-3 1-3 3-3 3 2 3 3"/>
              <path d="M12 21c0-1 1-3 3-3s3 2 3 3-1 3-3 3-3-2-3-3"/>
            </svg>
          </div>
          <h3>Análisis de Procesos</h3>
          <p>Análisis impulsado por IA de todo tu flujo de trabajo empresarial para identificar cuellos de botella e ineficiencias.</p>
        </div>
        
        <div class="card fade-in">
          <div class="feature-icon">
            <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
            </svg>
          </div>
          <h3>Automatización Inteligente</h3>
          <p>Identifica procesos que pueden ser automatizados y obtén recomendaciones de implementación.</p>
        </div>
        
        <div class="card fade-in">
          <div class="feature-icon">
            <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M3 3v18h18"/>
              <path d="M18.7 8l-5.1 5.2-2.8-2.7L7 14.3"/>
            </svg>
          </div>
          <h3>Insights de Rendimiento</h3>
          <p>Obtén insights detallados y recomendaciones accionables para mejorar el rendimiento de tu negocio.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- How It Works Section -->
  <section class="section">
    <div class="container">
      <div class="section-header text-center mb-4">
        <h2>Cómo Funciona</h2>
        <p class="text-large">Simple, potente e inteligente</p>
      </div>
      
      <div class="grid grid-2">
        <div class="process-step fade-in">
          <div class="step-number">1</div>
          <h3>Conecta tus Sistemas</h3>
          <p>Integra con tus herramientas y flujos de trabajo existentes. Nuestra IA analiza de forma segura tus procesos empresariales.</p>
        </div>
        
        <div class="process-step fade-in">
          <div class="step-number">2</div>
          <h3>Análisis de IA</h3>
          <p>Nuestros algoritmos avanzados de IA analizan tus datos para identificar patrones, cuellos de botella y oportunidades de optimización.</p>
        </div>
        
        <div class="process-step fade-in">
          <div class="step-number">3</div>
          <h3>Obtén Recomendaciones</h3>
          <p>Recibe informes detallados con insights accionables y recomendaciones de automatización adaptadas a tu negocio.</p>
        </div>
        
        <div class="process-step fade-in">
          <div class="step-number">4</div>
          <h3>Implementa y Optimiza</h3>
          <p>Aplica las recomendaciones y observa cómo se dispara la eficiencia de tu negocio. El monitoreo continuo asegura un rendimiento óptimo.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="section section-cta" id="contact">
    <div class="container">
      <div class="cta-content text-center">
        <h2>¿Listo para Transformar tu Negocio?</h2>
        <p class="text-large">Únete a las empresas que ya han optimizado sus procesos con IA</p>
        <div class="cta-actions">
          <a href="#" class="btn">Iniciar Análisis Gratuito</a>
          <a href="#" class="btn btn-secondary">Programar Demo</a>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .hero-content {
    max-width: 800px;
    margin: 0 auto;
  }
  
  .hero-title {
    font-size: 4rem;
    margin-bottom: 1.5rem;
    text-align: center;
  }
  
  .hero-subtitle {
    text-align: center;
    margin-bottom: 2rem;
    opacity: 0.9;
  }
  
  .hero-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }
  
  .section-header {
    max-width: 600px;
    margin: 0 auto;
  }
  
  .feature-icon {
    width: 64px;
    height: 64px;
    background: var(--accent-gradient);
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
    color: var(--space-white);
  }
  
  .process-step {
    text-align: center;
    padding: 2rem;
  }
  
  .step-number {
    width: 60px;
    height: 60px;
    background: var(--accent-gradient);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--space-white);
    margin: 0 auto 1.5rem;
  }
  
  .section-cta {
    background: radial-gradient(circle at center, rgba(6, 182, 212, 0.1) 0%, transparent 70%);
  }
  
  .cta-content {
    max-width: 600px;
    margin: 0 auto;
  }
  
  .cta-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-top: 2rem;
  }
  
  @media (max-width: 768px) {
    .hero-title {
      font-size: 2.5rem;
    }
    
    .hero-actions {
      flex-direction: column;
      align-items: center;
    }
    
    .hero-actions .btn {
      width: 100%;
      max-width: 300px;
    }
    
    .cta-actions {
      flex-direction: column;
      align-items: center;
    }
    
    .cta-actions .btn {
      width: 100%;
      max-width: 300px;
    }
  }
  
  /* Animation delays for staggered fade-in */
  .fade-in:nth-child(1) { animation-delay: 0.1s; }
  .fade-in:nth-child(2) { animation-delay: 0.2s; }
  .fade-in:nth-child(3) { animation-delay: 0.3s; }
  .fade-in:nth-child(4) { animation-delay: 0.4s; }
</style> 